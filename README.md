# vw-map-updater
Update VW/TomTom navigation maps on older 16GB SD cards by automatically removing unwanted regions. Extracts archives, optimizes size, and copies to your card. NixOS-native with nix-shell dependencies.
